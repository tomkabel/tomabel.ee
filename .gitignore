# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.bolt/prompt
.bolt/config.json

# Build artifacts
tsconfig.tsbuildinfo
mydatabase.db
package-lock.json
ruvector.db

# KDE folder metadata
.directory

# Local dev tooling
.agents/
.claude/
.claude-flow/
.commandcode/
.kilocode/
skills-lock.json

# Local research and notes
research/
# GitHub Pages route dirs (see scripts/spa-routes.mjs) — 'research/' above
# would otherwise ignore the pub/research/ SPA route shell
!pub/research/
scroll-codrops.txt
tutorials.txt

# Nested projects / vendored deps
lenis/
openspec/
tomabel/
drafts/
# Added by code-review-graph
.code-review-graph/

# Leaked/exported cookie jars — never commit
cookie.txt
*.cookies
cookies.txt

# Private reputation-strategy drafts and research artifacts.
# These normally live one directory up, OUTSIDE this worktree, so git never
# sees them. This is a guard in case one is ever copied in while working:
# they contain personal strategy and unpublished drafts and must never land
# in the public site repo.
phase5*.md
phase5-source/
phase*-handoff-prompt.md
identity-rebrand-plan.md

# Design-audit screenshots (26MB, regenerate with docs/design-audit/shoot.py)
docs/design-audit/screens/
